version: "3.9"

services:
  backend:
    build: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - 8001:8000
    container_name: fashion-backend

  nginx:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
